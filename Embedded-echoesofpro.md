# Echoes 0f Pro(Embedded):
    This is an embedded ctf challenge.
    >I opened the chall.sr(given file) file in pulse view(sigrok frontend) to visualize the waveform
    >In pulse view , I selected the protocol as UART
    >I adjusted the settings such RX=D0 and TX=D1 and set a baud rate to 9600 until readable text appear
    >So after that I could see the flag in decoded UART output and the flag is wired{sp4rtan_ph1sh_hims3lf_&_c4ll_him_hack3r}
    
## TOOLS USED:
    >unzip and cat commands were used
    >PulseView(GUI tool from sigrok suite)
